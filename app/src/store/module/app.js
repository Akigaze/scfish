const initialState = {
  apiURL: "http://localhost:8088",
}

const reducer = (state = initialState, action) => {

  switch (action.type) {
    default:
      return state
  }

}

export default reducer
