import {createBrowserHistory} from "history"

const history = createBrowserHistory()

// export const unListen = history.listen((location, action) => {
//
// })
//
// export const unBlock = history.block((location, action) => {
//
// })

export default history